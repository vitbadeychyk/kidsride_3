-- ============================================================================
-- KidsRide — міграція: додати колонку user_agent до таблиці product_views
-- Виконати у Supabase: SQL Editor → New query → вставити → Run
-- ----------------------------------------------------------------------------
-- Після виконання міграції завантажте оновлений product.html на сервер.
-- Нові перегляди товарів починуть зберігати user_agent відвідувача.
-- Стара статистика (без user_agent) відображатиметься як "Невідомо".
-- ============================================================================

-- Додати колонку user_agent (якщо ще не існує)
ALTER TABLE public.product_views
  ADD COLUMN IF NOT EXISTS user_agent text;

-- Опціональний індекс для швидшої фільтрації (не обов'язково)
-- CREATE INDEX IF NOT EXISTS product_views_ua_idx ON public.product_views (user_agent);
